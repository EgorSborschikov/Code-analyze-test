# Todo Application
**Todo Application** - это простое консольное приложение для управления списком дел, построенное на основе библиотеки классов с использованием средств анализа кода.

## 📦 Структура проекта
```shell
C:.
├───.github/
│   └───workflows/          
|       └───ci-analysis.yml # GitHub Actions workflows
├───SampleApp              # Консольное приложение
│   ├───bin/                # Скомпилированные бинарники
│   ├───obj/                # Промежуточные файлы сборки
│   └───Program.cs # Исходный код консольного приложения
├───Todo.Core              # Библиотека классов с логикой приложения
│   ├───bin/                # Скомпилированные бинарники
│   ├───obj/                # Промежуточные файлы сборки
│   ├───TodoItem.cs # Логика работы с элементом списка
│   └───TodoList.cs # Логика выполнения команд для списка
└───Todo.Test              # Модульные тесты
│   ├───bin/             # Скомпилированные бинарники тестов
│   ├───obj/               # Промежуточные файлы сборки
│   └───UnitTests.cs # Исходный код модульных тестов
```

## 🛠 Требования
- [.NET 8.0 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)
- (Опционально) [Visual Studio 2022](https://visualstudio.microsoft.com/) или [VS Code](https://code.visualstudio.com/)

## 🚀 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone git@github.com:EgorSborschikov/Code-analyze-test.git

cd TodoSolusion
```

### 2. Сборка проекта
``` bash
dotnet build
```

### 3. Запуск консольного приложения
```bash
dotnet run --project SampleApp
```

### 4. Запуск тестов
```bash
dotnet test
```

## 📝 Использование
Консольное приложение поддерживает следующие команды:
- [x] add (добавить новую задачу)
- [x] list (показать все задачи)
- [x] remove (удалить задачу по ID)
- [x] find (найти задачу по подстроке)
- [x] exit (выйти из приложения)

## 🔧 Стандарты кода
Проект использует следующие иструменты для анализа кода:
-  **Microsoft.CodeAnalysis.NetAnalyzers** — стандартные анализаторы кода от Microsoft.
-  **StyleCop.Analyzers** — анализаторы для соблюдения стиля кода.
-  инструкции выявления нарушений через **.editorconfig.base** (мягкий набор правил) и **.editorconfig.strict** (строгий набор правил)

Анализаторы подключены в проект библиотеки в файле **.csproj**
```xml
<ItemGroup>
  <PackageReference Include="Microsoft.CodeAnalysis.NetAnalyzers" Version="9.0.0" PrivateAssets="all" />
  <PackageReference Include="StyleCop.Analyzers" Version="1.1.118" PrivateAssets="all" />
</ItemGroup>
```